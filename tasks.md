# Website Optimization Tasks (May 2026)

## 🔴 High Priority (Immediate Impact)
- [x] **Fix Sitemap/Canonical Mismatch:** Update `sitemap.xml` to use clean URLs (remove `.html`) to match the canonical tags in the `<head>` of each page.
- [x] **Functional Contact Form:** Replace the placeholder `https://formspree.io/f/enquiry` in `contact.html` with a valid Formspree endpoint ID.
- [x] **Merge Duplicate Schema:** Consolidate the two separate `LocalBusiness` JSON-LD blocks in `index.html` into a single, comprehensive object.

## 🟡 Medium Priority (Growth & Polish)
- [ ] **Content Refresh (May 2026):** Add at least one new event entry or "Coming Soon" teaser to `events.html` and `portfolio.html` to show the business is active past March.
- [x] **Unique Social Previews:** Generate and assign unique `og:image` assets for `services.html`, `portfolio.html`, and `about.html`.
- [x] **Create `llms-full.txt`:** Compile a comprehensive markdown file at the root containing all service details, full FAQs, and business credentials for deep AI ingestion.

## 🔵 Low Priority (SEO/AEO Refinement)
- [x] **Voice Search Optimization:** Add `SpeakableSpecification` to the JSON-LD schema on the Home and Services pages.
- [x] **Improve Article Semantics:** Convert `portfolio-case` divs in `portfolio.html` to semantic `<article>` tags.
- [x] **FAQ Distribution:** Add relevant FAQ schema blocks to the `services.html` page to capture "How much does it cost?" and "How long is a session?" queries.
- [x] **Documentation Cleanup:** Update `docs/SEO-AI.md` to mark "Footer JS Rendering" as resolved and add these new tasks to the tracking list.
